* validate passed args
* handle all errors
* Support GHE
* add README

CLI

* make front end
* allow options to be specified in a TOML file

Long term

* what does dry-running look like?
* multi-repo support

Failure scenario:
Org or repo don't exist
Repo is empty (github doesn't allow forking)